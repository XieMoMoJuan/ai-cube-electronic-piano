# AI Cube 电子琴

基于 STC8H8K64U（AI Cube）开发板制作的简易电子琴。

项目通过 ADC 轮询多个触摸/按键通道，根据检测结果选择音阶，并使用定时器中断和 PWM 输出对应频率的方波，实现 C5～B5 七个音符的播放。

## 主要功能

- 7 路按键/触摸输入检测
- ADC 阈值判断
- 定时器中断生成 PWM
- C5～B5 音阶播放
- USB 初始化与数据轮询

## 使用方法

使用 Keil C51 打开 `aicubepro1.uvproj`，编译后将生成的 HEX 文件烧录到开发板。

核心程序位于 `Sources/main.c`。
